[project]
name = "03-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastmcp",
    "requests>=2.32.5",
    "minsearch",
]
